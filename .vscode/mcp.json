{
  // MCP Server Configuration — Fedora Tweaks (project-specific servers only)
  //
  // Shared servers (github, context7, fetch, memory, etc.) are defined in
  // the primary workspace folder (Veo Prompt Generator) and available here.
  //
  "servers": {
    "loofi-workflow": {
      "type": "stdio",
      "command": "python3",
      "args": [
        "-u",
        "scripts/mcp_workflow_server.py"
      ],
      "cwd": "${workspaceFolder}"
    },
    "loofi-agent-sync": {
      "type": "stdio",
      "command": "python3",
      "args": [
        "-u",
        "scripts/mcp_agent_sync_server.py"
      ],
      "cwd": "${workspaceFolder}"
    }
  }
}